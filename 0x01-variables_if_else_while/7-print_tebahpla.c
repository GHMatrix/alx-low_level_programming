#include <stdio.h>
/**
 * main - Program prints lowercase alphabets in reverse
 * Return: 0 (Success)
 */
int main(void)
{
	char i;

	i = 'z';
	while
		(i >= 'a') {
			putchar(i);
			i--;
		}
	putchar('\n');
	return (0);
}
