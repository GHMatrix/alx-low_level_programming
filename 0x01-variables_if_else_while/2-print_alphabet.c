#include <stdio.h>
/**
 * main - Program that prints alphabets in lower case with new line
 * Return: 0 (Success)
 */
int main(void)
{
	char d;

	d = 'a';
	while
		(d <= 'z') {
			putchar(d);
			d++;
		}
	putchar('\n');
	return (0);
}

