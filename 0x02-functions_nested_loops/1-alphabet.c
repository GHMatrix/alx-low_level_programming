#include "main.h"
/**
 * print_alphabet - Program prints alphabet in lowercase
 */
void print_alphabet(void)
{
	char alphabet = 'a';

	for (a <= 'z'; alphabet++)
	{
		_putchar(alphabet);
		_putchar('\n');
	}
	return (void);
