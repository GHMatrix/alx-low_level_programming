#include "main.h"
/**
 * _abs - Prints absolute value of figure
 * @n: integer being checked
 * Return: absolute of the integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
