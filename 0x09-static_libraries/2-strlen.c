#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

/**
 * _strlen - Program returns length of string
 * @s: string to be considered
 * Return: length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}

